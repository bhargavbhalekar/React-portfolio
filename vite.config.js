import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// Replace with your GitHub repo name if using GitHub Pages
export default defineConfig({
  base: 'React-portfolio',
  plugins: [react()],
})
